name: Transfer codeowner of a world
description: Hand off CODEOWNERS responsibility for an apworld to a different GitHub account.
title: "[codeowner-transfer] <apworld>: <current> → <new>"
labels: ["codeowner-transfer"]
body:
  - type: markdown
    attributes:
      value: |
        Use this template to request that the per-world author listed in `.github/CODEOWNERS`
        for a specific apworld be changed to a different GitHub account.

        This issue does **not** auto-edit CODEOWNERS — a maintainer reviews the request and
        applies the change manually. Both the current and the proposed new codeowner should
        confirm the transfer in the comments before the maintainer merges.
  - type: input
    id: apworld
    attributes:
      label: APWorld
      description: Filename stem under `worlds/` (e.g. `myclgm` for `worlds/myclgm.json`). Lowercase letters, digits, and underscores only.
      placeholder: "myclgm"
    validations:
      required: true
  - type: input
    id: current
    attributes:
      label: Current codeowner
      description: GitHub handle currently listed for `apworld` in `.github/CODEOWNERS`. Without the leading `@`.
      placeholder: "Malon"
    validations:
      required: true
  - type: input
    id: new
    attributes:
      label: Proposed new codeowner
      description: GitHub handle to take over. Must be a real GitHub user (or a team in `org/team` form). Without the leading `@`.
      placeholder: "Zelda"
    validations:
      required: true
  - type: textarea
    id: reason
    attributes:
      label: Reason for transfer
      description: Optional context. e.g. original author no longer maintaining the world, fork ownership change, account migration.
      placeholder: "Original author asked me to take over."
    validations:
      required: false
  - type: checkboxes
    id: confirm
    attributes:
      label: Maintainer checklist
      description: For the maintainer who applies the change.
      options:
        - label: Both current and new codeowner have confirmed the transfer in the comments.
          required: false
        - label: The new codeowner is a real GitHub account (or team).
          required: false
        - label: Edited `.github/CODEOWNERS` and merged the change.
          required: false
        - label: Verified that the apworld releases are still working as expected.
          required: false
